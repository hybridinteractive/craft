{% import '_macros/atoms' as atom %}

{%- minify -%}
    <section>
        {{- atom.article_image(
            content.image[0] ?? placeholder.articlePlaceholder[0], 
            content.image[0].title, 
            content.ratio, 
            content.caption ?? content.image[0].caption, 
            content.image[0].author ?? null, 
            content.image[0].source ?? null,
            colour
            ) -}}
    </section>
{%- endminify -%}